# ============================================================
#   CONFIGURACIÓN PRINCIPAL DE LA APLICACIÓN
# ============================================================

application.name=clases-online
application.mode=dev
%prod.application.mode=prod
application.secret=DwTqo9A0UOQLCqlT0gTqVbZpgNgTGLO1zKSkvLxXV8kkOSAAPio65TPOV2oEn25h

# ============================================================
#   IDIOMAS Y FECHAS
# ============================================================
date.format=yyyy-MM-dd

# ============================================================
#   CONFIGURACIÓN DEL SERVIDOR
# ============================================================
# Puerto por defecto (Play 1.x)
# http.port=9000
# http.address=127.0.0.1

# ============================================================
#   BASE DE DATOS H2 PERSISTENTE (GUARDADA EN DISCO)
# ============================================================
# ⚙️ Guarda los datos en el archivo ./data/play.h2.db
#    Igual que jdbc:h2:mem:play, pero sin perderlos al cerrar.
db.default.url=jdbc:h2:file:./data/play;MODE=MYSQL;LOCK_MODE=0
db.default.driver=org.h2.Driver
db.default.user=sa
db.default.pass=

# Hibernate (JPA)
jpa.default.ddl=create-drop
# jpa.default.debugSQL=true   # (actívalo si quieres ver las consultas SQL)

# ============================================================
#   SESIONES Y COOKIES
# ============================================================
# application.session.maxAge=1h
# application.session.secure=false

# ============================================================
#   ARCHIVOS Y CACHE
# ============================================================
attachments.path=data/attachments

# ============================================================
#   CORREO (DESACTIVADO POR DEFECTO)
# ============================================================
mail.smtp=mock

# ============================================================
#   ENTORNO DE PRUEBAS
# ============================================================
%test.application.mode=dev
%test.db.url=jdbc:h2:mem:play;MODE=MYSQL;LOCK_MODE=0
%test.jpa.ddl=create
%test.mail.smtp=mock
